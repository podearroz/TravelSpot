name: travelspot
description: Flutter frontend mínimo para o projeto TravelSpot
publish_to: 'none'
version: 0.1.0
environment:
  sdk: '>=2.18.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^2.6.0
  firebase_auth: ^4.3.0
  google_sign_in: ^6.0.1
  supabase_flutter: ^1.4.0
  http: ^0.13.6
  image_picker: ^0.8.7+5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
